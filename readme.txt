This project converts JSON code into YAML format through a web-based UI.

It also provides the following features:

1. Removes events and scripts, and masks sensitive tokens
2. Applies clean and readable formatting
3. Supports literal blocks for multiline content
